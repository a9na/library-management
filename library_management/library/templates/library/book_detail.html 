<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ book.title }} - Details</title>
    
    {% load static %}

    <link rel="stylesheet" href="{% static 'library/styles.css' %}">
</head>
<body>
    <!-- Header -->
    <header>
        <nav>
            <ul>
                <li><a href="{% url 'home' %}">Home</a></li>
                <li><a href="{% url 'book_list' %}">Book List</a></li>
                {% if user.is_authenticated %}
                    <li><a href="{% url 'logout' %}">Logout</a></li>
                    {% if user.user_type == 'client' %}
                        <li><a href="{% url 'member_dashboard' %}">Dashboard</a></li>
                    {% elif user.user_type == 'privileged' %}
                        <li><a href="{% url 'privileged_dashboard' %}">Privileged Dashboard</a></li>
                    {% elif user.user_type == 'admin' %}
                        <li><a href="{% url 'admin_dashboard' %}">Admin Dashboard</a></li>
                    {% endif %}
                {% else %}
                    <li><a href="{% url 'login' %}">Login</a></li>
                    <li><a href="{% url 'register' %}">Register</a></li>
                {% endif %}
            </ul>
        </nav>
    </header>

    <main>
        <h1>{{ book.title }}</h1>
        <p><strong>Author:</strong> {{ book.author }}</p>
        <p><strong>Description:</strong> {{ book.description }}</p>
        
        {% if book.image %}
            <img src="{{ book.image.url }}" alt="Book image" width="300">
        {% else %}
            <p>No image available</p>
        {% endif %}

        <p><strong>ISBN:</strong> {{ book.isbn }}</p>
        <p><strong>Availability:</strong> {% if book.available %} Available {% else %} Unavailable {% endif %}</p>
    </main>

    <!-- Footer -->
    <footer>
        <p>&copy; 2025 Library Management System</p>
    </footer>
</body>
</html>
